
import cookie_s from './cookie_s';
import localStorage_s from './localStorage_s';
import sessionStorage_s from './sessionStorage_s';

const cacheSimple = {
    cookie_s,
    localStorage_s,
    sessionStorage_s
}
export default cacheSimple;